
from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="SolarCalendar",
    install_requires=[
        "jdatetime >= 3.8.0",
    ],

)